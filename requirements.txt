# Wasatch Intelligence — Python dependencies
#
# Core tool runs entirely on Python standard library.
# Optional dependencies improve specific features:

# Required for longitude_sync.py (Longitude Network PostgreSQL integration)
# psycopg2-binary>=2.9.0
# Install: pip install psycopg2-binary

# Optional — improves brokerage roster scraping in movement_tracker.py
# beautifulsoup4>=4.12.0
# Install: pip install beautifulsoup4

# Quick setup for all optional deps:
#   pip install psycopg2-binary beautifulsoup4
